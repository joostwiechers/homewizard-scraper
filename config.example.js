const config = {
    // The folder the files should be written to.
    dataFileLocation: './data/',

    // The interval in seconds to read and write.
    readInterval: 10,

    // API endpoint of the homewizard unit.
    url: 'http://127.0.0.1/api/v1/data',
}

module.exports = config
